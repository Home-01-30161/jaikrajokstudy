# JaiKrajok — team07 (Phase 1: LINE + web chat, no DB yet)
#
# base = 20000 + (7 - 1) * 10 = 20060
#   20060 -> frontend  (static files served by the api app at "/")
#   20061 -> api       (LINE webhook + /health)
# Both point at the same container:8000 because one FastAPI app serves both.
#
# TEAM / BASE / BASE_1 are injected by GitLab CI variables.
name: ${TEAM:-team07}

x-logging: &logging
  driver: json-file
  options:
    max-size: "50m"
    max-file: "3"

x-common: &common
  restart: unless-stopped
  logging: *logging
  networks: [app]

services:
  api:
    <<: *common
    build: ./api
    ports:
      # frontend (static files served by this same app at "/")
      - "127.0.0.1:${BASE:-20060}:8000"
      # api
      - "127.0.0.1:${BASE_1:-20061}:8000"
    env_file:
      - .env
    environment:
      ROOT_PATH: /api
      APP_ENV: production
      LOG_LEVEL: INFO
      TEAM: ${TEAM:-team07}
    volumes:
      - /data/hack/${TEAM:-team07}/uploads:/app/uploads
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "2.0"

networks:
  app:
    driver: bridge
