# Env / secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Local media / smoke outputs
tmp/
*.log
scripts/out/

# Analysis / scratch artifacts
api/*.txt
api/*.md
api/*.json
claude.md

# Local SQLite store. Created at runtime; the deployed container keeps its own.
api/data/
*.db
*.db-shm
*.db-wal

# Frontend build tooling. The built output in api/app/frontend/ is committed so
# the image needs no Node stage, but node_modules never is.
node_modules/
web/dist/
